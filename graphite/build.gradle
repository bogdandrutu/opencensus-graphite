description = 'OpenCensus Graphite'

dependencies {
    compile libraries.guava,
            libraries.jackson_annotations,
            libraries.opencensus_api

    compileOnly libraries.auto_value

    signature "org.codehaus.mojo.signature:java18:1.0@signature"
}
